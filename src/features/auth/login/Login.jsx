import Card from '../../../components/card/Card.jsx';

const Login = () => {
    return (
        <div className="max-width max-height flex-container center">
            <Card className="auth__card flex-container column"></Card>
        </div>
    );
};

export default Login;
